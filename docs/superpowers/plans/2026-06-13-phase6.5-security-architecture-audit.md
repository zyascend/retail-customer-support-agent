# Phase 6.5: Security Architecture Audit & Documentation — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 7-layer write guard 变成可展示、可验证的安全架构证据，通过文档扩展 + 截图 + 覆盖审计。

**Architecture:** 纯文档扩展 — 重写 `docs/portfolio-architecture.md` 第 4 节（6 个子节），生成 2 张 Workbench 截图。不改代码、不补测试、不写工具。现有 30/30 eval 保持绿色。

**Tech Stack:** Markdown, Workbench (React + FastAPI), 截图工具（macOS 自带）

---

## File Structure

| 文件 | 操作 | 职责 |
|------|------|------|
| `docs/portfolio-architecture.md` | Modify — 重写第 4 节 | 安全架构深度参考（面试官单文档入口） |
| `docs/demo-screenshots/guard-block-evidence.png` | Create | guard block timeline 截图 + 中文标注 |
| `docs/demo-screenshots/write-audit-evidence.png` | Create | successful write timeline 截图 + 中文标注 |
| `app/agent/guard.py` | Read-only — 审计参考 | 26 个 block reason 的源代码 |
| `tests/test_agent_core.py` | Read-only — 审计参考 | WriteGuardTests 单元测试 |
| `app/eval/cases.py` | Read-only — 审计参考 | generalized_mvp eval cases |

---

## 审计基线：26 个 Block Reason 完整映射

以下是在写计划前完成的逐行审计结果。每个 block reason 标注了 guard.py 行号、触发条件、单元测试覆盖和 eval case 覆盖。

### Layer 0 — Action Type Validation (guard.py:42-45)

| # | Block Reason | 触发条件 | 代码行 | 单元测试 | Eval Case | 状态 |
|---|-------------|---------|--------|---------|-----------|------|
| 1 | `unsupported_in_mvp` | action 在 DEFERRED_WRITE_ACTIONS 中 | :42-43 | 无 | 无 | ❌ 防御性（当前集合为空） |
| 2 | `unknown_write_action` | action 不在 WRITE_ACTIONS 中 | :44-45 | 无 | 无 | ❌ 防御性（ToolGateway 已过滤） |

### Layer 1 — Authentication (guard.py:46-47)

| # | Block Reason | 触发条件 | 代码行 | 单元测试 | Eval Case | 状态 |
|---|-------------|---------|--------|---------|-----------|------|
| 3 | `authentication_required` | state.authenticated_user_id 为空 | :46-47 | `test_blocks_unauthenticated_write` | 无（eval 流程中 auth 总是先完成） | ⚠️ 仅单元测试 |

### Layer 2 — Explicit Confirmation (guard.py:48-52)

| # | Block Reason | 触发条件 | 代码行 | 单元测试 | Eval Case | 状态 |
|---|-------------|---------|--------|---------|-----------|------|
| 4 | `explicit_confirmation_required` | confirmed=False | :48-52 | `test_gateway_blocks_write_without_confirmation` | `deny_cancel_confirmation`, `deny_modify_payment_confirmation`, `deny_modify_address_confirmation` | ✅ 双覆盖 |

### Layer 3 — Ownership Validation (guard.py:109-124)

| # | Block Reason | 触发条件 | 代码行 | 单元测试 | Eval Case | 状态 |
|---|-------------|---------|--------|---------|-----------|------|
| 5 | `ownership_violation` | order.user_id ≠ auth user；modify_user_address target ≠ self | :116, :122-123 | `test_blocks_wrong_user_order_mutation` | `block_wrong_user_order_access` | ✅ 双覆盖 |
| 6 | `order_not_found` | order_id 在 DB 中不存在 | :120-121 | 无 | 无 | ❌ 防御性（context_loader 先校验） |

### Layer 4 — Read-Before-Write (guard.py:126-136)

| # | Block Reason | 触发条件 | 代码行 | 单元测试 | Eval Case | 状态 |
|---|-------------|---------|--------|---------|-----------|------|
| 7 | `read_before_write_required` | order/user 未在 loaded_context 中 | :130-131, :134-135 | 无 | 无 | ❌ 防御性（context_loader 先加载） |

### Layer 5 — Policy Compliance (guard.py:138-221)

| # | Block Reason | 触发条件 | 代码行 | 单元测试 | Eval Case | 状态 |
|---|-------------|---------|--------|---------|-----------|------|
| 8 | `non_pending_order_cannot_be_cancelled` | cancel 非 pending 订单 | :143-144 | `test_blocks_invalid_order_statuses` | `block_cancel_processed_order` | ✅ 双覆盖 |
| 9 | `invalid_cancel_reason` | cancel reason 不在允许集合中 | :145-146 | 无 | 无 | ❌ 防御性（parser 总提取有效 reason） |
| 10 | `non_pending_order_cannot_be_modified` | modify address/items/payment 非 pending | :148-149, :162-163, :168-169 | 无 | `block_modify_items_non_pending_order`, `block_modify_payment_processed_order` | ⚠️ 仅 eval |
| 11 | `non_delivered_order_cannot_be_returned` | return 非 delivered 订单 | :151-152 | `test_blocks_invalid_order_statuses` | `block_return_pending_order` | ✅ 双覆盖 |
| 12 | `non_delivered_order_cannot_be_exchanged` | exchange 非 delivered 订单 | :154-155 | 无 | 无 | ❌ 防御性 |
| 13 | `exchange_item_count_mismatch` | exchange item_ids ≠ new_item_ids 数量 | :156-157 | 无 | 无 | ❌ 防御性 |
| 14 | `replacement_item_count_mismatch` | modify_items 数量不匹配 | :183-184 | 无 | 无 | ❌ 防御性 |
| 15 | `order_item_not_found` | item_id 不在订单中 | :191-192 | 无 | 无 | ❌ 防御性 |
| 16 | `replacement_item_not_found` | new_item_id 不在 DB variant 中 | :194-195 | 无 | 无 | ❌ 防御性 |
| 17 | `replacement_item_product_mismatch` | 新旧 item 不属于同一 product | :196-197 | `test_blocks_item_replacement_across_products`, `test_blocks_exchange_across_products` | `block_item_product_mismatch`, `block_exchange_product_mismatch` | ✅ 双覆盖 |
| 18 | `replacement_item_unavailable` | 新 item 的 available=False | :198-199 | 无 | `block_item_unavailable`, `block_exchange_unavailable_replacement` | ⚠️ 仅 eval |
| 19 | `payment_method_not_owned` | 支付方式不属于用户 | :210-211 | `test_blocks_payment_method_not_owned` | `block_payment_not_owned` | ✅ 双覆盖 |
| 20 | `same_payment_method` | 新旧支付方式相同 | :213-214 | 无 | `block_same_payment_method` | ⚠️ 仅 eval |
| 21 | `gift_card_balance_insufficient` | 礼品卡余额不足 | :219-220 | 无 | `block_payment_insufficient_gift_card` | ⚠️ 仅 eval |
| 22 | `user_not_found` | modify_user_address 目标用户不存在 | :174-175 | 无 | 无 | ❌ 防御性（context_loader 先校验） |

### Layer 6 — Resource Locks (guard.py:243-262)

| # | Block Reason | 触发条件 | 代码行 | 单元测试 | Eval Case | 状态 |
|---|-------------|---------|--------|---------|-----------|------|
| 23 | `duplicate_write_lock` | 同一 lock key 已存在 | :246-247 | 无 | 无 | ❌ 单会话无并发写 |
| 24 | `order_already_cancelled_or_locked` | order cancel lock 已存在 | :250-251 | 无 | 无 | ❌ 单会话无并发写 |
| 25 | `order_items_already_modified` | order modify_items lock 存在时再做 cancel/modify | :252-254 | 无 | 无 | ❌ 单会话无并发写 |
| 26 | `item_already_returned_or_exchanged` | 同一 item 已有 return/exchange lock | :260-261 | 无 | 无 | ❌ 单会话无并发写 |

### Layer 7 — Idempotency (guard.py:73-80)

| # | 机制 | 代码行 | 单元测试 | Eval Case | 状态 |
|---|------|--------|---------|-----------|------|
| 27 | idempotency_key 生成 (stable_hash) | :73-80 | `test_idempotency_key_changes_with_arguments` | 所有 write eval cases 均验证 idempotency key 存在 | ✅ 双覆盖 |

### 汇总

- **总 block reason + 行为**: 27
- **✅ 双覆盖（单元 + eval）**: 8 (authentication_required, explicit_confirmation_required, ownership_violation, non_pending_order_cannot_be_cancelled, non_delivered_order_cannot_be_returned, replacement_item_product_mismatch, payment_method_not_owned, idempotency_key)
- **⚠️ 仅 eval 覆盖**: 5 (non_pending_order_cannot_be_modified, replacement_item_unavailable, same_payment_method, gift_card_balance_insufficient, 含 Layer 1 的 auth)
- **➖ 仅单元测试覆盖**: 0
- **❌ 无覆盖**: 12 (全部为防御性代码 — 要么被上游节点 shadow，要么 DEFERRED_WRITE_ACTIONS 当前为空，要么单会话无并发写场景)

---

### Task 1: 创建分支并审计 Guard 代码

**Files:**
- Read: `app/agent/guard.py` (285 lines)
- Read: `tests/test_agent_core.py` WriteGuardTests + GatewayAndTraceTests (lines 288-490, 775-790)
- Read: `app/eval/cases.py` 所有 expected_guard_block_reason 行

- [ ] **Step 1: 创建 Phase 6.5 分支**

```bash
git checkout -b phase6.5-security-architecture-audit
```

- [ ] **Step 2: 逐行审计 guard.py — 确认 26 个 block reason 清单**

打开 `app/agent/guard.py`，按以下 checklist 逐行确认每个 block reason：

```text
check() 方法 (lines 34-87):
  □ :42-43  unsupported_in_mvp          — DEFERRED_WRITE_ACTIONS 检查
  □ :44-45  unknown_write_action         — WRITE_ACTIONS 白名单检查
  □ :46-47  authentication_required      — state.authenticated_user_id 检查
  □ :48-52  explicit_confirmation_required — confirmed 参数检查
  □ :58-60  ownership_violation / order_not_found — _validate_ownership()
  □ :61-63  read_before_write_required   — _validate_read_before_write()
  □ :64-66  [policy block reasons]       — _validate_policy()

_validate_ownership() (lines 109-124):
  □ :116     ownership_violation         — modify_user_address 非本人
  □ :121     order_not_found             — order_id 在 DB 中不存在
  □ :123     ownership_violation         — order.user_id != auth user

_validate_read_before_write() (lines 126-136):
  □ :131     read_before_write_required  — order 未在 loaded_context
  □ :135     read_before_write_required  — user 未在 loaded_context

_validate_policy() (lines 138-176):
  □ :144     non_pending_order_cannot_be_cancelled
  □ :146     invalid_cancel_reason
  □ :149     non_pending_order_cannot_be_modified  (address)
  □ :152     non_delivered_order_cannot_be_returned
  □ :155     non_delivered_order_cannot_be_exchanged
  □ :157     exchange_item_count_mismatch
  □ :160     [delegates to _validate_item_replacements]
  □ :163     non_pending_order_cannot_be_modified  (items)
  □ :166     [delegates to _validate_item_replacements]
  □ :169     non_pending_order_cannot_be_modified  (payment)
  □ :172     [delegates to _validate_payment_change]
  □ :175     user_not_found (modify_user_address)

_validate_item_replacements() (lines 178-200):
  □ :184     replacement_item_count_mismatch
  □ :192     order_item_not_found
  □ :195     replacement_item_not_found
  □ :197     replacement_item_product_mismatch
  □ :199     replacement_item_unavailable

_validate_payment_change() (lines 202-221):
  □ :207     user_not_found              — order 的用户不在 DB
  □ :211     payment_method_not_owned
  □ :214     same_payment_method
  □ :220     gift_card_balance_insufficient

_lock_conflict() (lines 243-262):
  □ :247     duplicate_write_lock
  □ :251     order_already_cancelled_or_locked
  □ :254     order_items_already_modified
  □ :261     item_already_returned_or_exchanged

Idempotency (lines 73-80):
  □ :73-80   idempotency_key 生成 (stable_hash) — 非 block reason，是去重机制
```

审计结论：**26 个 block reason + 1 个 idempotency key 生成 = 27 个独立行为**，与设计 spec 一致。

- [ ] **Step 3: 确认单元测试覆盖映射**

对照 `tests/test_agent_core.py` WriteGuardTests (line 288-460) 和 GatewayAndTraceTests (line 462-490)：

```text
test_blocks_unauthenticated_write          → authentication_required ✅
test_blocks_wrong_user_order_mutation      → ownership_violation ✅
test_blocks_invalid_order_statuses         → non_pending_order_cannot_be_cancelled ✅
                                           → non_delivered_order_cannot_be_returned ✅
test_idempotency_key_changes_with_arguments → idempotency_key 生成 ✅
test_blocks_item_replacement_across_products → replacement_item_product_mismatch ✅
test_blocks_payment_method_not_owned       → payment_method_not_owned ✅
test_blocks_exchange_across_products       → replacement_item_product_mismatch ✅
test_gateway_blocks_write_without_confirmation → explicit_confirmation_required ✅
test_guard_block_response_is_user_readable → user-readable response (集成测试，非直接 guard 单元) ➖
```

审计结论：**7 个 WriteGuardTests + 1 个 GatewayAndTraceTests**，覆盖 6 个独立 block reason + idempotency 生成。

- [ ] **Step 4: 确认 Eval Case 覆盖映射**

从 `app/eval/cases.py` 提取所有有 `expected_guard_block_reason` 的 case：

```text
block_cancel_processed_order       → non_pending_order_cannot_be_cancelled
block_return_pending_order         → non_delivered_order_cannot_be_returned
block_wrong_user_order_access      → ownership_violation (implied)
block_item_product_mismatch        → replacement_item_product_mismatch
block_item_unavailable             → replacement_item_unavailable
block_payment_not_owned            → payment_method_not_owned
block_payment_insufficient_gift_card → gift_card_balance_insufficient
block_same_payment_method          → same_payment_method
block_modify_items_non_pending_order → non_pending_order_cannot_be_modified
block_modify_payment_processed_order → non_pending_order_cannot_be_modified
block_exchange_product_mismatch     → replacement_item_product_mismatch
block_exchange_unavailable_replacement → replacement_item_unavailable
```

加确认相关 case（deny/change）：
```text
deny_cancel_confirmation           → explicit_confirmation_required (implicit)
deny_modify_payment_confirmation   → explicit_confirmation_required (implicit)
deny_modify_address_confirmation   → explicit_confirmation_required (implicit)
changed_confirmation_discards_pending_action → explicit_confirmation_required (implicit)
changed_modify_items_confirmation  → explicit_confirmation_required (implicit)
```

审计结论：**12 个 guard block eval cases + 5 个 confirmation cases**，覆盖 8 个独立 block reason + confirmation 行为。

- [ ] **Step 5: 提交审计结果（可选中间提交）**

```bash
git add -A
git commit -m "docs: Phase 6.5 guard 全量审计 — 26 block reason 映射完成"
```

---

### Task 2: 扩展 portfolio-architecture.md 第 4 节

**Files:**
- Modify: `docs/portfolio-architecture.md` — 替换第 4 节（当前 lines 65-78）

- [ ] **Step 1: 阅读现有第 4 节及前后文**

阅读 `docs/portfolio-architecture.md`：
- 第 1-3 节（lines 1-63）了解语气和格式
- 第 4 节（lines 65-78）确认待替换的精确内容
- 第 5-7 节（lines 80-137）确认替换后衔接

关键格式约定：
- 使用中文叙述，英文专有术语保留原名
- 表格用 Markdown pipe table
- 代码引用格式：`app/agent/guard.py:42-43`
- 列表用 `- ` 开头
- 子标题用 `###` 或 `####`

- [ ] **Step 2: 替换第 4 节 — 写入 4.1-4.6 完整内容**

在 `docs/portfolio-architecture.md` 中，将当前 lines 65-78：

```markdown
## 4. 7-Layer Write Guard

`WriteActionGuard` 在 `app/agent/guard.py` 中实现，在**每一个写工具调用前**执行：

| Layer | 检查项 | 失败处理 | 实现位置 |
|-------|--------|----------|----------|
| 1. Authentication | 用户必须已登录 | block: `auth_required` | guard.py |
| 2. Confirmation | write 必须 `confirmed=True` | block: `confirmation_required` | guard.py |
| 3. Ownership | 订单必须属于认证用户 | block: `wrong_user` | guard.py |
| 4. Read-before-write | 订单必须先加载到 context | block: `order_not_loaded` | guard.py |
| 5. Policy | 订单状态、商品可用性、支付归属、礼品卡余额 | block: 具体原因 | guard.py |
| 6. Resource Locks | 同一资源不允许多个并发/重复写入 | block: `resource_locked` | guard.py |
| 7. Idempotency | 基于 hash 的幂等性 key | 去重而非 block | guard.py |

**关键不变量**: tools never call the guard directly。`ToolGateway` (`app/tools/gateway.py`) 是唯一入口点。
```

替换为以下完整内容（注意：old_string 必须精确匹配，包括反引号和空格）：

**old_string**:
```
## 4. 7-Layer Write Guard

`WriteActionGuard` 在 `app/agent/guard.py` 中实现，在**每一个写工具调用前**执行：

| Layer | 检查项 | 失败处理 | 实现位置 |
|-------|--------|----------|----------|
| 1. Authentication | 用户必须已登录 | block: `auth_required` | guard.py |
| 2. Confirmation | write 必须 `confirmed=True` | block: `confirmation_required` | guard.py |
| 3. Ownership | 订单必须属于认证用户 | block: `wrong_user` | guard.py |
| 4. Read-before-write | 订单必须先加载到 context | block: `order_not_loaded` | guard.py |
| 5. Policy | 订单状态、商品可用性、支付归属、礼品卡余额 | block: 具体原因 | guard.py |
| 6. Resource Locks | 同一资源不允许多个并发/重复写入 | block: `resource_locked` | guard.py |
| 7. Idempotency | 基于 hash 的幂等性 key | 去重而非 block | guard.py |

**关键不变量**: tools never call the guard directly。`ToolGateway` (`app/tools/gateway.py`) 是唯一入口点。
```

**new_string**:
```markdown
## 4. 7-Layer Write Guard

`WriteActionGuard` 在 `app/agent/guard.py` 中实现，在**每一个写工具调用前**由 `ToolGateway` (`app/tools/gateway.py`) 统一调度。共 26 个 block reason + 1 个 idempotency key 生成，分布在 8 个校验步骤中。

### 4.1 设计哲学

写操作安全是交易型 Agent 的核心竞争力。7 层设计遵循三个原则：

- **纵深防御**：每层解决一类独立的安全威胁。即使上游层被绕过（如 parser 错误提取了 order_id），下游层仍会拦截（ownership 校验会拒绝不属于该用户的订单）。
- **Guard 是最终裁决层**：LLM 可以建议写操作，但不能决定写操作是否安全。ToolGateway 强制执行 guard check，LLM 没有绕过路径。
- **deny-wins 短路**：任何一层返回 block 即立即终止，不执行后续检查。false negative（拒绝合法操作）优于 false positive（允许非法操作）—— 用户遇到 false negative 可以重新请求，但 false positive 可能造成不可逆的损失。

### 4.2 架构全景

```
用户请求 → action_planner 生成 pending_action
              ↓
         ToolGateway.execute()
              ↓
     WriteActionGuard.check(state, db, action, confirmed)
              ↓
    ┌─────────────────────────────────────────┐
    │ Layer 0: Action Type Validation         │ → unsupported_in_mvp / unknown_write_action
    │ Layer 1: Authentication                 │ → authentication_required
    │ Layer 2: Explicit Confirmation          │ → explicit_confirmation_required
    │ Layer 3: Ownership Validation           │ → ownership_violation / order_not_found
    │ Layer 4: Read-Before-Write              │ → read_before_write_required
    │ Layer 5: Policy Compliance              │ → 15 个具体 policy block reason
    │ Layer 6: Resource Locks                 │ → 4 个 lock conflict reason
    │ Layer 7: Idempotency                    │ → idempotency_key 生成（去重，不 block）
    └─────────────────────────────────────────┘
              ↓
    allowed=True  → tool 执行 → DB mutation + write audit
    allowed=False → 返回 block_reason，不执行 tool，DB 不变
```

**关键不变量**: tools never call the guard directly。ToolGateway 是唯一入口点，所有 write tool call 必须通过 `gateway.execute()` → `guard.check()`。

### 4.3 分层详解

#### 4.3.0 Layer 0 — Action Type Validation (`guard.py:42-45`)

在进入 7 层安全校验之前，先验证 action 本身是否合法：

- `unsupported_in_mvp`：action 在 `DEFERRED_WRITE_ACTIONS` 集合中（当前为空，为未来扩展预留）
- `unknown_write_action`：action 不在 `WRITE_ACTIONS` 白名单中（`app/agent/action_specs.py:WRITE_ACTION_NAMES` 派生）

这一层是防御性代码 —— ToolGateway 已经在调度层过滤了非写操作，但 guard 内部保留二次校验作为纵深防御。

#### 4.3.1 Layer 1 — Authentication (`guard.py:46-47`)

`state.authenticated_user_id` 必须非空。用户在 pipeline 的 `identity_resolver` 节点完成认证后，该字段被设置。如果跳过认证直接发起写操作，此层拦截。

Block reason: `authentication_required`

#### 4.3.2 Layer 2 — Explicit Confirmation (`guard.py:48-52`)

`confirmed` 参数必须为 `True`。Agent 在 `action_planner` 生成写操作后，会先通过 `response_generator` 向用户展示操作摘要并请求确认。用户回复 "yes" / "confirm" 后，`confirmation.py` 解析确认意图，runtime 将 `confirmed=True` 传入 guard。

Block reason: `explicit_confirmation_required`

这是用户可感知的最直接安全层 —— 没有用户的明确确认，任何写操作都不会执行。

#### 4.3.3 Layer 3 — Ownership Validation (`guard.py:109-124`)

验证操作目标属于当前认证用户：

- **订单操作**：`order.user_id` 必须等于 `state.authenticated_user_id`（`guard.py:122-123`）
- **用户地址操作**（`modify_user_address`）：`user_id` 参数必须等于认证用户（`guard.py:113-116`）
- **订单存在性**：order_id 必须在 DB 中存在（`guard.py:120-121`）

Block reason: `ownership_violation`, `order_not_found`

#### 4.3.4 Layer 4 — Read-Before-Write (`guard.py:126-136`)

操作目标必须已加载到 `state.loaded_context`：

- 订单操作：`order_id` 必须在 `state.loaded_context.orders` 中（`guard.py:130-131`）
- 用户地址操作：`user_id` 必须在 `state.loaded_context.users` 中（`guard.py:134-135`）

这一层确保 Agent 在修改数据前已经读取并理解了当前状态。`context_loader` 节点在 pipeline 中位于 `action_planner` 之前，正常流程中此层不会触发，但作为防御层保留。

Block reason: `read_before_write_required`

#### 4.3.5 Layer 5 — Policy Compliance (`guard.py:138-221`)

最复杂的校验层，按写操作类型分派到不同的策略校验方法：

**订单状态校验**：
- `cancel_pending_order` → 订单状态必须为 `pending`（`guard.py:143-144`）
- `modify_pending_order_address/items/payment` → 订单状态必须为 `pending`（`guard.py:148-149, 162-163, 168-169`）
- `return_delivered_order_items` → 订单状态必须为 `delivered`（`guard.py:151-152`）
- `exchange_delivered_order_items` → 订单状态必须为 `delivered`（`guard.py:154-155`）

Block reason: `non_pending_order_cannot_be_cancelled`, `non_pending_order_cannot_be_modified`, `non_delivered_order_cannot_be_returned`, `non_delivered_order_cannot_be_exchanged`

**Cancel reason 校验**（`guard.py:145-146`）：
- reason 只允许 `"no longer needed"` 和 `"ordered by mistake"`

Block reason: `invalid_cancel_reason`

**商品替换校验**（`_validate_item_replacements`, `guard.py:178-200`）：
- 新旧 item 数量必须匹配（`guard.py:183-184`）
- 旧 item 必须在订单中（`guard.py:191-192`）
- 新 item 必须在 DB variant 中存在（`guard.py:194-195`）
- 新旧 item 必须属于同一 product（`guard.py:196-197`）
- 新 item 必须 available（`guard.py:198-199`）

Block reason: `replacement_item_count_mismatch`, `exchange_item_count_mismatch`, `order_item_not_found`, `replacement_item_not_found`, `replacement_item_product_mismatch`, `replacement_item_unavailable`

**支付方式校验**（`_validate_payment_change`, `guard.py:202-221`）：
- 支付方式必须属于当前用户（`guard.py:210-211`）
- 新支付方式不能与当前相同（`guard.py:213-214`）
- 如果使用 gift card，余额必须覆盖订单金额（`guard.py:219-220`）

Block reason: `payment_method_not_owned`, `same_payment_method`, `gift_card_balance_insufficient`

**用户地址校验**（`guard.py:173-175`）：
- 目标用户必须在 DB 中存在

Block reason: `user_not_found`

#### 4.3.6 Layer 6 — Resource Locks (`guard.py:243-262`)

防止同一资源上的并发/重复写入。每次成功的写操作会向 `state.write_locks` 追加一个 lock key：

- `duplicate_write_lock`：完全相同的 lock key 已存在（`guard.py:246-247`）
- `order_already_cancelled_or_locked`：订单已被取消或有 cancel lock（`guard.py:250-251`）
- `order_items_already_modified`：订单已修改过 items（`guard.py:252-254`）
- `item_already_returned_or_exchanged`：同一 item 已有 return/exchange lock（`guard.py:260-261`）

当前 Workbench 单会话 demo 中这些 lock 不会触发（每个 case 只有一次写操作），但在多轮对话或多 Agent 协作场景中，它们是防止重复操作的最后一道防线。

Block reason: `duplicate_write_lock`, `order_already_cancelled_or_locked`, `order_items_already_modified`, `item_already_returned_or_exchanged`

#### 4.3.7 Layer 7 — Idempotency (`guard.py:73-80`)

基于 `session_id + tool_name + arguments + resource_lock` 生成 `stable_hash` 作为 idempotency key。这一层不产生 block reason（它是去重机制，不是阻断机制），但它是安全架构的关键组成部分 —— 即使 guard 被重复调用，相同的操作也会产生相同的 idempotency key，tool executor 可根据此 key 去重。

Idempotency key 包含在 `ToolCallRecord` 和 trace artifact 中，用于审计追溯。

### 4.4 覆盖率矩阵

以下矩阵展示 26 个 block reason + idempotency key 的测试和 eval 覆盖状态：

| # | Layer | Block Reason / 行为 | 单元测试 | Eval Case | 状态 |
|---|-------|--------------------|---------|-----------|------|
| 1 | 0 | `unsupported_in_mvp` | — | — | ❌ 防御性 |
| 2 | 0 | `unknown_write_action` | — | — | ❌ 防御性 |
| 3 | 1 | `authentication_required` | `test_blocks_unauthenticated_write` | — | ⚠️ 仅单元 |
| 4 | 2 | `explicit_confirmation_required` | `test_gateway_blocks_write_without_confirmation` | `deny_cancel_confirmation` 等 5 个 | ✅ |
| 5 | 3 | `ownership_violation` | `test_blocks_wrong_user_order_mutation` | `block_wrong_user_order_access` | ✅ |
| 6 | 3 | `order_not_found` | — | — | ❌ 防御性 |
| 7 | 4 | `read_before_write_required` | — | — | ❌ 防御性 |
| 8 | 5 | `non_pending_order_cannot_be_cancelled` | `test_blocks_invalid_order_statuses` | `block_cancel_processed_order` | ✅ |
| 9 | 5 | `invalid_cancel_reason` | — | — | ❌ 防御性 |
| 10 | 5 | `non_pending_order_cannot_be_modified` | — | `block_modify_items_non_pending_order`, `block_modify_payment_processed_order` | ⚠️ 仅 eval |
| 11 | 5 | `non_delivered_order_cannot_be_returned` | `test_blocks_invalid_order_statuses` | `block_return_pending_order` | ✅ |
| 12 | 5 | `non_delivered_order_cannot_be_exchanged` | — | — | ❌ 防御性 |
| 13 | 5 | `exchange_item_count_mismatch` | — | — | ❌ 防御性 |
| 14 | 5 | `replacement_item_count_mismatch` | — | — | ❌ 防御性 |
| 15 | 5 | `order_item_not_found` | — | — | ❌ 防御性 |
| 16 | 5 | `replacement_item_not_found` | — | — | ❌ 防御性 |
| 17 | 5 | `replacement_item_product_mismatch` | `test_blocks_item_replacement_across_products`, `test_blocks_exchange_across_products` | `block_item_product_mismatch`, `block_exchange_product_mismatch` | ✅ |
| 18 | 5 | `replacement_item_unavailable` | — | `block_item_unavailable`, `block_exchange_unavailable_replacement` | ⚠️ 仅 eval |
| 19 | 5 | `payment_method_not_owned` | `test_blocks_payment_method_not_owned` | `block_payment_not_owned` | ✅ |
| 20 | 5 | `same_payment_method` | — | `block_same_payment_method` | ⚠️ 仅 eval |
| 21 | 5 | `gift_card_balance_insufficient` | — | `block_payment_insufficient_gift_card` | ⚠️ 仅 eval |
| 22 | 5 | `user_not_found` | — | — | ❌ 防御性 |
| 23 | 6 | `duplicate_write_lock` | — | — | ❌ 单会话 |
| 24 | 6 | `order_already_cancelled_or_locked` | — | — | ❌ 单会话 |
| 25 | 6 | `order_items_already_modified` | — | — | ❌ 单会话 |
| 26 | 6 | `item_already_returned_or_exchanged` | — | — | ❌ 单会话 |
| 27 | 7 | idempotency_key 生成 | `test_idempotency_key_changes_with_arguments` | 所有 write eval cases | ✅ |

**汇总**:
- ✅ 双覆盖（单元 + eval）: **8** — 核心安全路径充分验证
- ⚠️ 仅 eval 覆盖: **5+1** — eval 层面完整，单元测试可后续补充
- ❌ 未覆盖: **12** — 全部为防御性代码或单会话限制，在正常 pipeline 流程中由上游节点保证不会触发

### 4.5 审计结论

#### 已知弱覆盖区域

以下 12 个 block reason 在当前测试和 eval 中无覆盖：

| 类别 | Block Reason | 为何可接受 |
|------|-------------|-----------|
| 被上游 shadow | `order_not_found`, `read_before_write_required`, `user_not_found` | `context_loader` 在 guard 之前运行，已校验目标存在性 |
| 被上游 shadow | `invalid_cancel_reason`, `exchange_item_count_mismatch`, `replacement_item_count_mismatch`, `order_item_not_found`, `replacement_item_not_found` | parser + action_planner 已校验参数合法性 |
| 防御性占位 | `unsupported_in_mvp`, `unknown_write_action` | 当前无 deferred actions，ToolGateway 已过滤非写操作 |
| 防御性占位 | `non_delivered_order_cannot_be_exchanged` | 行为与 return 对称，测试覆盖了 return 侧 |
| 单会话限制 | `duplicate_write_lock`, `order_already_cancelled_or_locked`, `order_items_already_modified`, `item_already_returned_or_exchanged` | 当前 Workbench 单会话 demo 中不会触发并发写；Phase 11（多轮 history）和 Phase 7（synthetic sandbox）为自然补充时机 |

#### 后续 Phase 中 guard 扩展检查清单

当 Phase 7 新增 `modify_pending_order_shipping_method` 写操作时：
- [ ] `action_specs.py` 新增 `WriteActionSpec`
- [ ] `_validate_policy()` 新增配送方式相关 block reason
- [ ] `_resource_lock()` 新增 lock key 生成
- [ ] 至少 1 个单元测试覆盖新增 block reason
- [ ] 至少 2 个 eval case（success + block）
- [ ] 更新本文档 4.4 覆盖率矩阵

### 4.6 证据展示

#### Guard Block 证据

![Guard Block Trace](../demo-screenshots/guard-block-evidence.png)

*Workbench 中 `block_wrong_user_order_access` case 的 timeline 截图。关键证据：① ToolGateway 返回 blocked 状态 ② block_reason = `ownership_violation` ③ before_db_hash = after_db_hash（no-write invariant）④ 无 write lock 生成*

#### Successful Write 证据

![Write Audit Trace](../demo-screenshots/write-audit-evidence.png)

*Workbench 中 `cancel_pending_order` case 的 timeline 截图。关键证据：① confirmation 通过（用户回复 "yes"）② guard check allowed=True ③ tool 执行成功，DB mutation 完成 ④ idempotency_key 和 resource_lock 记录在 ToolCallRecord 中 ⑤ write audit log 可追溯*

#### 当前关键指标

| 指标 | 值 | 来源 |
|------|-----|------|
| `mutation_error_rate` | 0.0 | `generalized_mvp` 30/30 pass |
| `guard_block_rate` | ~0.27 | 30 cases 中 ~8 个为 guard block case |
| no-write invariant | 100% | 所有 guard block case 的 before/after DB hash 一致 |
| `tool_call_success_rate` | 1.0 | 所有 write case 工具调用全部成功 |
```

**验证替换完整性**：确认新内容中 section 编号为 `## 4.` → `### 4.1` ~ `### 4.6`，节标题与设计 spec 一致。

- [ ] **Step 3: 检查第 5 节衔接**

确认第 5 节 `## 5. ToolGateway & Action Specs` 在新第 4 节之后仍然格式正确、无断裂。如果第 5 节中引用了 guard 相关内容，确认引用仍有效。

- [ ] **Step 4: 提交**

```bash
git add docs/portfolio-architecture.md
git commit -m "docs: Phase 6.5 重写 portfolio-architecture 第 4 节 — 7 层 guard 详解 + 覆盖率矩阵 + 审计结论"
```

---

### Task 3: 生成演示截图

**Files:**
- Create: `docs/demo-screenshots/guard-block-evidence.png`
- Create: `docs/demo-screenshots/write-audit-evidence.png`

- [ ] **Step 1: 启动 Workbench**

```bash
# 终端 1: 启动后端
cd /Users/theyang/Documents/ai/retail-customer-support-agent
uv run phase4-workbench
```

```bash
# 终端 2: 启动前端
cd /Users/theyang/Documents/ai/retail-customer-support-agent/workbench
npm run dev
```

打开浏览器访问 Workbench（默认 http://localhost:5173）。

- [ ] **Step 2: 截取 Guard Block 证据**

1. 在 Workbench case selector 中选择 `🛡️ 写保护阻止` → `阻止访问他人订单`（`block_wrong_user_order_access`）
2. 运行 case，等待 timeline 渲染完成
3. 展开 timeline 中的 guard block 事件，确保 block reason 可见
4. 确认 Business State 面板中 before_db_hash = after_db_hash
5. 使用 macOS 截图工具（Cmd+Shift+4）截取全屏
6. 用 Preview 或标注工具添加中文标注：
   - 红色箭头指向 guard block 事件 → 标注 "block_reason: ownership_violation"
   - 绿色箭头指向 DB hash → 标注 "before_db_hash = after_db_hash (no-write invariant)"
   - 蓝色箭头指向 Tool Call Record → 标注 "status: blocked, 无 write lock 生成"
7. 保存为 `docs/demo-screenshots/guard-block-evidence.png`

- [ ] **Step 3: 截取 Successful Write 证据**

1. 在 Workbench case selector 中选择 `✅ 成功写操作` → `取消待处理订单`（`cancel_pending_order`）
2. 运行 case，观察确认流程
3. 确保 timeline 中能看到：
   - confirmation 步骤（用户回复 "yes"）
   - guard check 步骤（allowed=True）
   - tool execution 步骤
   - DB mutation 状态变化
4. 使用 macOS 截图工具截取全屏
5. 用标注工具添加中文标注：
   - 绿色箭头指向 confirmation → 标注 "用户确认：'yes' → confirmed=True"
   - 蓝色箭头指向 guard pass → 标注 "guard.check() → allowed=True"
   - 橙色箭头指向 tool execution → 标注 "cancel_pending_order 执行成功"
   - 紫色箭头指向 ToolCallRecord → 标注 "idempotency_key + resource_lock 已记录"
   - 红色箭头指向 DB state → 标注 "before_db_hash ≠ after_db_hash (mutation 已发生)"
6. 保存为 `docs/demo-screenshots/write-audit-evidence.png`

- [ ] **Step 4: 验证截图文件**

```bash
ls -la docs/demo-screenshots/guard-block-evidence.png docs/demo-screenshots/write-audit-evidence.png
```

确认两个文件都存在且大小 > 50KB（非空白截图）。

- [ ] **Step 5: 提交**

```bash
git add docs/demo-screenshots/guard-block-evidence.png docs/demo-screenshots/write-audit-evidence.png
git commit -m "docs: Phase 6.5 安全架构证据截图 — guard block + successful write"
```

---

### Task 4: 验证

**Files:** 无修改

- [ ] **Step 1: 运行单元测试**

```bash
uv run python -m pytest tests/ -q
```

Expected: 全部通过，0 failed。

- [ ] **Step 2: 运行 Ruff check**

```bash
uv run ruff check .
```

Expected: 零输出（无错误）。

- [ ] **Step 3: 运行 Ruff format check**

```bash
uv run ruff format --check .
```

Expected: 零输出（无待格式化文件）或 `22 files already formatted`。

- [ ] **Step 4: 运行 generalized_mvp eval**

```bash
uv run phase2-eval --subset generalized_mvp --trials 1 --no-progress --json
```

Expected: `"pass_1": 1.0`, `"pass_k": 1.0`（30/30）。

- [ ] **Step 5: 构建 Workbench 前端**

```bash
cd workbench && npm run build
```

Expected: 构建成功，零错误。

- [ ] **Step 6: 文档链接验证**

检查 `docs/portfolio-architecture.md` 中所有：
- 文件路径引用指向存在的文件
- 代码行号引用与实际代码一致（`guard.py:42-43` 等）
- 截图路径引用正确（`../demo-screenshots/guard-block-evidence.png`）

```bash
# 快速验证关键文件存在
ls app/agent/guard.py app/tools/gateway.py app/agent/action_specs.py
ls docs/demo-screenshots/guard-block-evidence.png docs/demo-screenshots/write-audit-evidence.png
```

- [ ] **Step 7: 提交最终验证结果**

```bash
git add -A
git commit -m "docs: Phase 6.5 验证通过 — 测试/eval/lint/build 全绿"
```

---

## 验收标准 Checklist

- [ ] `portfolio-architecture.md` 第 4 节有完整的 4.1-4.6 子节
- [ ] 27 个 guard 行为全部列出，每个有对应的覆盖状态
- [ ] 覆盖率矩阵汇总正确（8 双覆盖 + 5-6 eval + 12 防御性）
- [ ] 审计结论标注了弱覆盖区域及可接受理由
- [ ] 2 张截图可用，中文标注关键证据点
- [ ] `uv run python -m pytest tests/ -q` 通过
- [ ] `uv run ruff check .` 通过
- [ ] `uv run ruff format --check .` 通过
- [ ] `uv run phase2-eval --subset generalized_mvp --trials 1 --no-progress --json` 报告 30/30
- [ ] `cd workbench && npm run build` 通过
- [ ] mutation_error_rate 在核心 eval 中为 0
